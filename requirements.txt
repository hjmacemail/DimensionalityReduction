numpy>=1.23
scipy>=1.9
pandas>=1.5
scikit-learn>=1.2
networkx>=3.0
matplotlib>=3.6
streamlit>=1.28
tabulate>=0.9          # enables Markdown/LaTeX table export in the analysis scripts

# Optional (heavy): the *true* VAE baseline. Left commented so cloud builds stay
# light and fast — without torch, the VAE baseline transparently falls back to a
# linear (SVD) autoencoder. Uncomment if you want the neural VAE.
# torch>=2.0
